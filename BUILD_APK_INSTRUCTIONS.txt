╔════════════════════════════════════════════════════════╗
║  📱 FraudShield - APK Build Instructions               ║
╚════════════════════════════════════════════════════════╝

✅ GOOD NEWS: Build files are ready!

═══════════════════════════════════════════════════════════

📋 OPTION 1: Using Build Script (Easiest)
═══════════════════════════════════════════════════════════

Simply double-click: build-apk.bat

This will:
1. Build the React app
2. Sync to Android
3. Open Android Studio

═══════════════════════════════════════════════════════════

📋 OPTION 2: Using Android Studio (Recommended)
═══════════════════════════════════════════════════════════

Step 1: Open Android Studio
   Command: npx cap open android

Step 2: Wait for Gradle Sync
   - Android Studio will automatically sync Gradle
   - Wait for "Gradle sync finished" notification
   - This may take 2-5 minutes first time

Step 3: Build APK
   - Go to: Build → Build Bundle(s) / APK(s) → Build APK(s)
   - OR press: Ctrl + Shift + A, then type "Build APK"
   - Wait for build to complete (2-5 minutes)
   - Click "locate" link in notification

Step 4: Find Your APK
   📂 Location: android\app\build\outputs\apk\debug\app-debug.apk

═══════════════════════════════════════════════════════════

📋 OPTION 3: Command Line (Advanced)
═══════════════════════════════════════════════════════════

cd android
gradlew assembleDebug

APK Location: app\build\outputs\apk\debug\app-debug.apk

═══════════════════════════════════════════════════════════

📱 INSTALLING APK ON YOUR PHONE
═══════════════════════════════════════════════════════════

Method A: Via USB (Fastest)

1. Enable USB Debugging:
   - Settings → About Phone → Tap "Build Number" 7 times
   - Settings → Developer Options → Enable "USB Debugging"

2. Connect phone via USB

3. Install:
   adb install android\app\build\outputs\apk\debug\app-debug.apk

   OR directly from Android Studio:
   npx cap run android


Method B: Transfer APK File

1. Copy app-debug.apk to your phone (via USB, Bluetooth, etc.)
2. Open file manager on phone
3. Tap the APK file
4. Allow "Install from Unknown Sources" if prompted
5. Install the app

═══════════════════════════════════════════════════════════

🔧 TROUBLESHOOTING
═══════════════════════════════════════════════════════════

❌ "SDK location not found"
   Solution: Android Studio should auto-configure this.
   If not, it will create android\local.properties automatically.

❌ "Gradle sync failed"
   Solution:
   cd android
   gradlew clean
   gradlew build

❌ "Java version mismatch"
   Solution: Install Java JDK 17
   Android Studio comes with its own JDK.

❌ White screen on app launch
   Solution: This shouldn't happen, but if it does:
   npm run build
   npx cap sync android
   Rebuild APK

═══════════════════════════════════════════════════════════

📊 BUILD STATUS
═══════════════════════════════════════════════════════════

✅ Dependencies installed
✅ React app built successfully
✅ Android platform added
✅ Web assets synced to Android
✅ Ready to build APK!

═══════════════════════════════════════════════════════════

🎯 NEXT STEPS
═══════════════════════════════════════════════════════════

1. Run: npx cap open android
2. Wait for Gradle sync in Android Studio
3. Build → Build APK
4. Install on your phone and test!

═══════════════════════════════════════════════════════════

📝 NOTES
═══════════════════════════════════════════════════════════

- This is a DEBUG APK (for testing)
- For production/Play Store, you need a SIGNED APK
- See DEPLOYMENT.md for production build instructions
- APK size: ~15-20 MB (debug version)

═══════════════════════════════════════════════════════════

🎉 You're almost there! Just open Android Studio and build!




